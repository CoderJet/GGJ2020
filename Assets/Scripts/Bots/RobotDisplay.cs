using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RobotDisplay : MonoBehaviour
{
    private GameObject HeadButton;
    private GameObject LeftArmButton;
    private GameObject RightArmButton;
    private GameObject LeftLegButton;
    private GameObject RightLegButton;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
